<div class="card">
    <div class="card-header bg-dark text-light">Level 3</div>
    <div class="card-body">
        <p>Using the SportsORM, complete all the following queries and show their results on Level3.cshtml.</p>
        <ol>
            <li>...all teams, past and present, that Samuel Evans has played with</li>
            <ul><b>Current Team</b></ul>

            <ul>@ViewBag.Sam.CurrentTeam.TeamName</ul>
            <ul><b>Past Teams</b></ul>

            @foreach (var t in ViewBag.Sam.AllTeams)
            {
                <ul>@t.TeamOfPlayer.TeamName</ul>

            }

            <li>...all players, past and present, with the Manitoba Tiger-Cats</li>
            <ul><b>Past Players</b></ul>
            @foreach (var t in ViewBag.AllWV.AllPlayers)
            {
                <ul>@t.PlayerOnTeam.FirstName @t.PlayerOnTeam.LastName</ul>

            }
            <ul><b>Current Players</b></ul>

            @foreach (var t in ViewBag.AllWV.CurrentPlayers)
            {
                <ul>@t.FirstName @t.LastName</ul>

            }

            <li>...all players who were formerly (but aren't currently) with the Wichita Vikings</li>
            <ul><b>Past Players</b></ul>
            @foreach (var t in ViewBag.AllCV.AllPlayers)
            {
                <ul>@t.PlayerOnTeam.FirstName @t.PlayerOnTeam.LastName</ul>

            }
            <li>...every team that Jacob Gray played for before he joined the Oregon Colts</li>
            <ul><b>Past Teams</b></ul>

            @foreach (var t in ViewBag.JG.AllTeams)
            {
                <ul>@t.TeamOfPlayer.TeamName</ul>

            }
            <li>...everyone named "Joshua" who has ever played in the Atlantic Federation of Amateur Baseball Players
            </li>
            @foreach (var t in ViewBag.Joshua)
            {
                <ul>@t.FirstName</ul>

            }

            <li>...all teams that have had 12 or more players, past and present.</li>
            @foreach (var t in ViewBag.TwelveOrMore)
            {
                <ul>@t.TeamName</ul>

            }

            <li>...all players, sorted by the number of teams they've played for</li>
            @foreach (var p in ViewBag.PlayersByTeamNum)
            {
                <ul>@p.FirstName @p.LastName #of Teams: @p.AllTeams.Count </ul>

            }
        </ol>
    </div>
</div>